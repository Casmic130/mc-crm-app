rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This CRM currently uses Firebase Authentication and browser localStorage.
    // Firestore is not used by the application, so client access stays denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
